import 'package:flutter/material.dart';

class Product {
  var imageUrl, title, description;
  var price, size, id;
  Product({
    this.id,
    this.imageUrl,
    this.title,
    this.price,
    this.description,
    this.size,
  });
}
