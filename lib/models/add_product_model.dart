import 'package:flutter/material.dart';

class AddNewProduct {
  var imageUrl, title, description;
  var price, size;
  AddNewProduct({
    this.imageUrl,
    this.title,
    this.price,
    this.description,
    this.size,
  });
}
